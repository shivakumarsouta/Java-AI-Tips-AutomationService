name: Daily Java Interview Tip Automation

on:
  schedule:
    # 4:30 AM UTC matches 10:00 AM IST (UTC + 5:30)
    - cron: '30 4 * * *'
  workflow_dispatch: # Allows you to manually trigger it from the GitHub website for testing

jobs:
  run-daily-tip:
    runs-on: ubuntu-latest

    # Grant permissions to write changes back to the repository
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build Shaded JAR with Maven
        run: mvn clean package

      - name: Run Tip Generator
        env:
          # Bind configurations securely from GitHub Secrets to the environment
          LLM_API_URL: ${{ secrets.LLM_API_URL }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_API_MODEL: ${{ secrets.LLM_API_MODEL }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_AUTH: ${{ secrets.SMTP_AUTH }}
          SMTP_STARTTLS: ${{ secrets.SMTP_STARTTLS }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          SMTP_FROM: ${{ secrets.SMTP_FROM }}
          SMTP_TO: ${{ secrets.SMTP_TO }}
          DB_URL: jdbc:sqlite:java_tips.db
          SCHEDULE_TIME: "10:00"
          SCHEDULE_TIMEZONE: "Asia/Kolkata"
        run: java -jar target/java-interview-tips-1.0-SNAPSHOT.jar --dry-run

      - name: Commit Database State and Archive Website
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Stage both the updated SQLite database and the regenerated archive website
          git add java_tips.db || true
          git add docs/index.html || true

          # Commit only if there are actual staged changes
          if ! git diff --cached --quiet; then
            git commit -m "Daily tip delivered — archive updated [skip ci]"
            git push
          else
            echo "No changes detected. Skipping commit."
          fi
